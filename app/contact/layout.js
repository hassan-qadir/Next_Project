export const metadata = {
  title: 'Contact',
}
 
export default function ContactLayout({ children }) {
 return (
    <html lang="en">
      <body>
        {children}
        </body>
    </html>
  )
}
